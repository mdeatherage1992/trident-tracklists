import React, {Component} from 'react';
import TracklistForm from './TracklistForm';

const Form = (props) => {

    return (
      <div>
  <TracklistForm />
</div>
    );
}

export default Form;

//change to functional component which dont have lifecycle methods
